<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- COLORI PRINCIPALI -->
    <attr name="titleBackground" format="reference|color" />
    <attr name="mainBackground" format="reference|color" />
    <attr name="navbarBackground" format="reference|color" />

    <!-- VIEW DIVISORI -->
    <attr name="pageDividingView" format="reference|color" />

    <!-- COLORE BOTTONI -->
    <attr name="okBtnBackground" format="reference|color" />
    <attr name="editBtnBackground" format="reference|color" />
    <attr name="notOkBtnBackground" format="reference|color" />
    <attr name="retryBtnBackground" format="reference|color" />

    <!-- PAGINA COLOR CORRECTION -->
    <attr name="ccTitleBackground" format="reference|color" />
    <attr name="radioGroupBackground" format="reference|color" />

    <!-- COLORI CERCHI -->
    <attr name="greyCircle" format="reference|color" />
    <attr name="redBorder" format="reference|color" />
    <attr name="yellowBorder" format="reference|color" />
    <attr name="greenBorder" format="reference|color" />

    <!-- FORM INPUT -->
    <attr name="formInputBackground" format="reference|color" />
</resources>
